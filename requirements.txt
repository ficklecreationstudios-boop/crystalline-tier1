# CPU-only Crystalline Tier 1 runtime requirements
# Python 3.10+ required

# Core numerical dependencies
numpy>=1.19.0
scipy>=1.5.0

# Optional development/test dependencies are declared in pyproject.toml.
# Optional visualization dependencies are declared in pyproject.toml.
# Numba is not a runtime dependency and is not used by the current Tier 1
# implementation. No Numba JIT or CUDA execution path is provided here.
